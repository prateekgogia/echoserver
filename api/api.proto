syntax = "proto3";

package api;

message echoMessage {
  string message = 1;
}

service Echo {
  rpc EchoRequest(echoMessage) returns (echoMessage) {}
}

service StatusStream {
  rpc Status(request) returns (stream statusResponse) {}
}

message request {
  
}

message statusResponse {
  string message = 1;
}